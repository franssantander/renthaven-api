info:
  name: Dashboard
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/property/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
