info:
  name: Store
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/property"
  body:
    type: json
    data: |-
      {
          // "tenant_business_id": 2,
          "name": "Test Property 2",
          "address": "Test addresss1",
          "type": "apartment"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
