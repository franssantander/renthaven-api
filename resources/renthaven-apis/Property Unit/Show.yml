info:
  name: Show
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/property-unit/:uuid"
  params:
    - name: uuid
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
