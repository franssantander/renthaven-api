info:
  name: Dashboard
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/property-unit/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
