info:
  name: Dashboard
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/renter-portal/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
