info:
  name: Recent Activity
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/dashboard/recent-activity"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
