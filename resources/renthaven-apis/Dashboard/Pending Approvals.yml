info:
  name: Pending Approvals
  type: http
  seq: 13

http:
  method: GET
  url: "{{base_url}}/dashboard/pending-approvals"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
