info:
  name: Read
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/notifications/:notification/read"
  params:
    - name: notification
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
