info:
  name: Mark Paid
  type: http
  seq: 5

http:
  method: PUT
  url: "{{base_url}}/ledger/:ledger_enty/pay"
  params:
    - name: ledger_enty
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
          "notes": "test notes"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
