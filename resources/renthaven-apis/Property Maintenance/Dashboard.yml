info:
  name: Dashboard
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/maintenance-request/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
