info:
  name: Forgot Password
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/forgot-password"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
